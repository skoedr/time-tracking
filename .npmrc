; v1.5 PR B — electron-builder + pnpm
; Without hoisted node_modules, transitive deps of native/electron packages
; (e.g. `ms` via `debug` via electron-updater) can't be resolved at runtime
; from inside app.asar. See: https://www.electron.build/tutorials/pnpm
node-linker=hoisted

{
  "name": "timetrack-streamdeck",
  "version": "0.1.0",
  "private": true,
  "description": "Stream Deck plugin for TimeTrack — toggle timers per client/project (#133)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "pack": "streamdeck pack com.timetrack.streamdeck.sdPlugin"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0"
  },
  "devDependencies": {
    "@elgato/utils": "^0.4.5",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
